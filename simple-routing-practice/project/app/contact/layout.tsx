export default function ContactLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return <section>
        <h1>From contact layout</h1>
        {children}
        </section>
  }